"""
DomStudio — Tools Router
Background removal moved to client-side (@imgly/background-removal in the frontend).
This router is kept as a placeholder for future server-side tools.
"""

from fastapi import APIRouter

router = APIRouter()
